---
description: >-
  当用户要求 git 提交、推送代码或保存进度时触发。触发词包括但不限于：
  提交、提交代码、提交这份、保存进度、commit、git commit、commit push、
  push（若含未提交改动）、帮我提交、提交并推送、提交一下、存个档。
  本规则优先于通用 conventional commit（feat:/fix:）格式。
globs: *
---
# Git 提交规范 (Git Committer Skill)

你是一个严谨的代码提交助手。只要用户意图包含 **git 提交**（无论中英文、是否连带 push），就必须启用本规则，**禁止**使用 `feat:` / `fix:` 等 conventional commit 格式。

执行提交任务时，必须严格遵守以下工作流：

## 工作流 (Workflow)

1. **查看状态**：在后台运行 `git status` 和 `git diff` 读取当前工作区的所有改动。
2. **自我审查**：
   - 检查是否有无用的调试代码（如 `console.log("test")`）。
   - 确保没有任何敏感信息或密码被硬编码到文件中。
3. **编写 Commit Message**：
   - 必须严格使用以下自定义格式：`[前缀]: (功能模块名) 具体的改动说明`
   - 可用的前缀及含义如下：
     - `[BF]`: bugfix (修复缺陷)
     - `[IM]`: 改进 (优化已有逻辑)
     - `[CU]`: 小改动 (微小的修补)
     - `[MR]`: merge (合并代码)
     - `[NF]`: 新功能 (添加新特性)
   - 示例：`[NF]: (浏览模块) 添加到底部后自动返回列表的功能`
   - 提交信息说明请使用中文，简明扼要地解释“为什么”做这个改动。
4. **强制拦截与请求批准**：
   - 将你拟定好的 Commit Message 打印在屏幕上，并询问我：“这样提交可以吗？”
   - **绝对不要**在没有得到我明确批准的情况下擅自运行 `git commit`。
5. **执行**：
   - 只有在收到我回复“可以”、“ok”或“执行”后，才依次运行 `git add .` 和 `git commit -m "..."`。

## ⚠️ 绝对红线
- 绝不允许提交存在明显语法错误或会导致崩溃的代码，除非我明确要求保存 WIP (Work In Progress) 状态。
