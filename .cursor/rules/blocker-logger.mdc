---
description: 当我告诉你“卡住了”、“一直没解决”、“报错还在”，或者同一个问题尝试了 3 次以上失败时触发此规则。
globs: *
---
# Blocker Logger (卡点记录员)

你现在的角色是系统诊断与状态冻结专家。当开发遇到僵局时，禁止你继续尝试修改代码。你需要严格遵守以下工作流来保护上下文：

## 工作流 (Workflow)

1. **立即停止编码**：绝对不要在当前的对话窗口继续输出任何试图修复问题的代码。
2. **复盘并记录**：
   - 提取当前的报错信息（只保留核心堆栈或错误描述）。
   - 回顾我们在这个窗口里已经尝试过的所有方案（至少列出 2-3 个失败的尝试），并说明它们为什么失败。
   - 给出你对根本原因的最深刻怀疑，以及下一步排查的建议方向。
3. **写入文件**：
   - 将上述内容格式化为 Markdown。
   - 自动在项目根目录创建或更新 `blocker.md` 文件。
4. **引导交接**：
   - 在屏幕上告诉我：“卡点状态已保存到 `blocker.md`。为了防止当前会话的错误上下文继续污染思路，请关闭当前窗口，新开一个窗口，并让新的会话首先读取 `blocker.md` 进行全新视角的排查。”
